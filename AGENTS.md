@/home/li/.codex/RTK.md

# AI 协作入口

开始工作前先读：

- `.ai_teamwork/START.md`

目标：用最少上下文了解当前项目进度、其他 AI 的认知和本轮任务。

## 用户命令约定

- 复杂启动命令必须提供参数可见、可逐行修改的完整长命令，并显式写齐所有续行反斜杠；不要用短包装脚本替代。用户需要自行调整参数。

- 默认按“新终端直接粘贴即可运行”提供命令；不得要求用户自行补参数、路径、续行符或替换占位符。

- 所有给用户的命令必须整段直接复制执行，包含工作目录、必要环境加载和完整参数。
- 每条命令写完整单行，或显式提供所有续行符；不能让用户自行补反斜杠或拼接 launch 文件名。
- 参数较多时优先使用短行并显式写好所有续行符，避免用户复制后把 launch 文件名或参数当成独立命令。
