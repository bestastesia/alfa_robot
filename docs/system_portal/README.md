# ALFA Robot 运控系统地图

这是一个纯静态、多页面的项目导航站，用来快速回答：

- 系统和外界如何交互？
- 内部有哪些主流程？
- 每个 ROS2 包消费什么、产出什么、当前状态如何？
- 哪些部分是稳定接口、过渡主包、实验工具、mock 或外部依赖？
- 目标架构的依赖方向、唯一事实源和调试代码晋升规则是什么？

## 打开方式

直接用浏览器打开：

```bash
xdg-open docs/system_portal/index.html
```

或者启动一个本地静态服务：

```bash
cd /mnt/mydisk/ALFA/alfa_robot/docs/system_portal
python3 -m http.server 8765
```

然后访问：`http://127.0.0.1:8765/`

## 文件结构

```text
docs/system_portal/
├── index.html        # 系统与外界交互总览
├── flows.html        # 业务/算法/碰撞/孪生流程
├── packages.html     # 包列表、搜索、过滤
├── package.html      # 单包详情页，通过 ?id=package_id 进入
├── architecture.html # 架构驾驶舱、职责矩阵和迁移路径
├── status.html       # 包状态看板
└── assets/
    ├── data.js       # 唯一主要数据源，新增包/流程时优先改这里
    ├── app.js        # 页面渲染逻辑
    └── site.css      # 视觉样式
```

## 更新规则

项目新增包、接口或流程时，优先更新 `assets/data.js`：

- 新包：追加到 `packages`。
- 新流程：追加到 `systemFlows`。
- 外部交互变化：更新 `externalActors`。
- 包状态变化：更新 `maturity`、`status` 和 `statusNotes`。

页面会自动重新渲染卡片、流程、搜索和状态看板。

架构规则的文字版见 `docs/运控/系统架构与包职责边界.md`。

## 架构驾驶舱的十轮迭代记录

本页不是新的生产前端，只是现有静态系统地图的表达升级；仍以 `assets/data.js` 为唯一内容源。

1. 将“目标架构长文”重排为甲方三分钟阅读顺序。
2. 增加系统能力宣言，先解释平台价值再展示代码边界。
3. 增加五阶段控制闭环，明确任务前向流与反馈闭环。
4. 增加四类能力指标，压缩系统规模信息。
5. 增加模型、状态、场景、证据四类唯一事实链。
6. 增加碰撞一致性、接口稳定、执行安全、可观测性保障区。
7. 重绘六层能力栈，强化“拥有 / 禁止”边界。
8. 增加交付视角与工程视角切换，兼顾汇报和代码追责。
9. 完成桌面、平板、移动端响应式布局和横向流程降级。
10. 增加打印模式、静态语法检查和资源可达验证，方便评审留档。
