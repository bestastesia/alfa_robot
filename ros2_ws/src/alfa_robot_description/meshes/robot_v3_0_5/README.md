# robot_v3.0.5 模型资产

来源：`/mnt/mydisk/ALFA/backpack/robot_v3.0.5`，上游源文件为 `底盘适配模型.STEP`。

- 保留上游 45 个 STL、原始 URDF 和导出元数据。
- 运行模型采用上游完整底座、固定升降架和左右七轴链。
- `link_002_joint` 在本轮 MoveIt 展示中固定为零位；双臂仍使用现有 14 轴控制接口。
- 源整机绕 X 轴旋转 +90°接入标准 `base_link`，并交换源左右链的语义映射，使左臂保持在 `+Y`、右臂保持在 `-Y`。
- 关节语义重命名为 `left_joint1..7`、`right_joint1..7`；J4 限位为 ±145°，其余限位沿用已确认值。
